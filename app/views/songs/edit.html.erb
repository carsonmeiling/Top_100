<h1><%=@song.name%></h1>
<h3><%=@song.length%></h3>

<%=form_for ([@artist, @song]) do |a|%>

  <%= a.label :name%>
  <%= a.text_field :name%>

  <%= a.label :length%>
  <%= a.text_field :length%>

  <%= a.submit%>
<% end %>